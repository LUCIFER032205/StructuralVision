name: structural_vision_ar
description: Building inspection - crack detection with AR overlay
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  supabase_flutter: ^2.8.0
  camera: ^0.11.0
  http: ^1.2.0
  ar_flutter_plugin_2: ^0.0.3
  vector_math: ^2.1.4
  printing: ^5.14.3
  image_picker: ^1.1.2
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
